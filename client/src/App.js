import React from "react";
import { BrowserRouter, Route } from "react-router-dom";
const App = (props) => {
  return (
    <BrowserRouter>
      <div>
        <Route></Route>
      </div>
    </BrowserRouter>
  );
};

export default App;
